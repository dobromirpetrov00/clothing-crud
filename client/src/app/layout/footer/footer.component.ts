import { Component } from '@angular/core';

/**
 * The `FooterComponent` is a standalone Angular component that represents the footer of the application.
 * It is responsible for rendering the footer content and any associated functionality.
 */
@Component({
  selector: 'app-footer',
  standalone: true,
  imports: [],
  templateUrl: './footer.component.html',
  styleUrl: './footer.component.scss',
})
export class FooterComponent {}
